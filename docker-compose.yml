services:
  sticker-server:
    build:
      context: ./server
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      GIPHY_API_KEY: ${GIPHY_API_KEY}
      STICKER_SERVER_HOST: 0.0.0.0
      STICKER_SERVER_PORT: 8080
